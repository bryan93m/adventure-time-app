import React from 'react';
import NavBar from "./NavBar"

function Header(props) {
    return (
        <div>
            <h1>Project Name</h1>
            <NavBar />
        </div>
    );
}

export default Header;